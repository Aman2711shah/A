name: wazeet_app
description: WAZEET Business Setup Platform
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # UI & Design
  cupertino_icons: ^1.0.6
  google_fonts: ^6.1.0
  flutter_svg: ^2.0.9
  lottie: ^3.3.2
  animations: ^2.0.11
  shimmer: ^3.0.0

  # State Management
  flutter_bloc: ^9.1.1
  equatable: ^2.0.5
  provider: ^6.1.1
  get_it: ^8.2.0

  # Network & API
  dio: ^5.4.0
  retrofit: ^4.0.3
  pretty_dio_logger: ^1.3.1
  connectivity_plus: ^7.0.0

  # Local Database
  sqflite: ^2.3.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.1

  # Authentication & Security
  flutter_secure_storage: ^10.0.0-beta.4
  local_auth: ^3.0.0
  crypto: ^3.0.3

  # File Handling & Documents
  file_picker: ^8.0.0+1
  image_picker: ^1.0.7
  # image_cropper: ^5.0.1
  path: ^1.8.3
  permission_handler: ^12.0.1
  open_filex: ^4.3.4

  # Firebase
  firebase_core: ^4.2.0
  firebase_analytics: ^12.0.3
  firebase_crashlytics: ^5.0.3
  firebase_messaging: ^16.0.3

  # Maps & Location
  google_maps_flutter: ^2.5.3
  geolocator: ^14.0.2
  geocoding: ^4.0.0

  # Payment
  flutter_stripe: ^12.0.2

  # Utilities
  intl: ^0.20.2
  url_launcher: ^6.2.2
  share_plus: ^12.0.1
  device_info_plus: ^12.2.0
  package_info_plus: ^8.3.1
  uuid: ^4.2.2
  cached_network_image: ^3.3.1
  flutter_dotenv: ^6.0.0
  json_annotation: ^4.9.0

  # Notifications
  flutter_local_notifications: ^19.5.0
  timezone: ^0.10.1

  # PDF Generation
  pdf: ^3.11.1
  printing: ^5.13.6

  # UI Components
  fl_chart: ^1.1.1
  dotted_border: ^3.1.0
  pin_code_fields: ^8.0.1
  smooth_page_indicator: ^1.1.0

  # Forms & Validation
  form_builder_validators: ^11.2.0
  flutter_form_builder: ^10.2.0

  # Navigation & Routing
  go_router: ^16.2.5
  
  # Storage
  shared_preferences: ^2.2.2
  firebase_auth: ^6.1.1
  cloud_firestore: ^6.0.3
  firebase_storage: ^13.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  fake_cloud_firestore: ^4.0.0
  mocktail: ^1.0.3
  
  # Code Generation
  build_runner: ^2.4.7
  retrofit_generator: ^8.0.6
  hive_generator: ^2.0.1
  json_serializable: ^6.7.1

flutter:
  uses-material-design: true
  
  assets:
    # NOTE: The following asset directories are temporarily disabled because they don't exist in the repo.
    # - assets/images/
    # - assets/icons/
    # - assets/animations/
    - assets/data/
